Django==5.1.4
Pillow==11.0.0
python-dotenv==1.0.1

# --- Produccion ---
# Estas versiones son las que se probaron en local. Render instalara las
# mismas, para que el sitio publicado se comporte igual que aqui.
gunicorn==26.0.0          # servidor web que ejecuta Django en Render
whitenoise==6.12.0        # sirve CSS, JS e imagenes sin servidor aparte
dj-database-url==3.1.2    # lee la URL de PostgreSQL que Render entrega
psycopg[binary]==3.3.4    # conector de PostgreSQL

# --- Solo si usas MySQL en tu computador ---
# Render usa PostgreSQL, asi que esta linea no hace falta para publicar.
# Descomentala si vas a trabajar con MySQL en local.
# mysqlclient==2.2.6
